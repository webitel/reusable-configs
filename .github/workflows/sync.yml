name: Sync files
on:
  push:
    branches: [ main ]

  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4.3.0

      - name: Sync files across repositories
        uses: webitel/reusable-workflows/actions/file-sync-action@004b64b4957eea3da1ad4ce70368507f0a269a3d
        with:
          GH_PAT: ${{ secrets.GH_PAT }}
          IS_FINE_GRAINED: true
          CONFIG_PATH: golang/sync.yml
          ASSIGNEES: kirychukyurii
          COMMIT_EACH_FILE: true
          OVERWRITE_EXISTING_PR: true
          DRY_RUN: false